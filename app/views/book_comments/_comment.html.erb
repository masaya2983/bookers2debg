 <p>コメント件数：<%=book.book_comments.count %></p>
  <% book. book_comments .each do |book_comments| %>
   <% book_comment_user = book_comment.user %>
  <% link_to(book_coment.user) do %>
  <%= image_tag book.user.get_profile_image(100,100) %>
  <% book_comment .user.name %>
  <% end %>
   </td>
   <div>
  <%= form_with model: [book, post_comment] do |f| %>
    <%= f.text_area :comment, rows: '5', placeholder: "コメントをここに" %>
    <%= f.submit "送信する" %>
  <% end %>
</div>
   <td>
   <% if post_comment.user == current_user %>
      <%= link_to "Destroy", book_comment_path(book_comment), method: :delete,　remote: true, class: "btn-sm btn-danger" %>
    <% end %>

    </td>
    </tr>
     <% end %>
</tbody>
